server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ress_database
    username: postgres
    password: 123494
  jpa:
    hibernate:
      ddl-auto: update
    openInView: false
    show_sql: true
    generate-ddl: true
  data:
    jpa:
      repositories.enabled: true